# OpenAPI provenance

`autoelevate-partner-api-beta.json` is the Partner API (beta) OpenAPI document, downloaded
2026-09-08 from the JSON link on https://partner-api-docs.autoelevate.com/ (`info.version` 1.0.0).

It is reference only. Nothing in this package is generated from it; the resource descriptions
are hand-written. When the API changes, download the new document here, diff it, and update the
descriptions by hand.
